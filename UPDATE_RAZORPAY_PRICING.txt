═══════════════════════════════════════════════════════════════
  FIX RAZORPAY PRICING - DEPLOY TO VPS
  VPS: 72.60.218.219
═══════════════════════════════════════════════════════════════

🔧 WHAT WAS FIXED:
───────────────────────────────────────────────────────────────
Razorpay payment amounts updated from test prices to actual:
  • Pro Plan: ₹101 → ₹1,000
  • Premium Plan: ₹201 → ₹1,499


═══════════════════════════════════════════════════════════════

🚀 DEPLOY FIX TO VPS (Copy-paste these commands)
───────────────────────────────────────────────────────────────

STEP 1: Connect to VPS
───────────────────────────────────────────────────────────────
ssh root@72.60.218.219


STEP 2: Navigate and pull latest code
───────────────────────────────────────────────────────────────
cd /root/Nextute-main
git pull origin main


STEP 3: Rebuild frontend (COPY ALL AT ONCE)
───────────────────────────────────────────────────────────────
cd frontend && \
rm -rf dist && \
npm run build && \
echo "✅ Frontend rebuilt with updated Razorpay pricing"


STEP 4: Copy to nginx (if using nginx)
───────────────────────────────────────────────────────────────
sudo cp -r dist/* /var/www/nextute/
sudo systemctl restart nginx


STEP 5: Restart PM2
───────────────────────────────────────────────────────────────
cd ..
pm2 restart all


STEP 6: Verify
───────────────────────────────────────────────────────────────
pm2 status
curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════

✅ VERIFY ON WEBSITE
───────────────────────────────────────────────────────────────

1. Visit: https://www.nextute.com
2. Hard refresh: Ctrl+Shift+R
3. Go to Mentorship page
4. Click "Book Session" on any mentor
5. Click "Choose Smart Guidance Plan" (Pro)
6. Check Razorpay popup shows: ₹1,000.00 (NOT ₹101)
7. Cancel and try Premium Plan
8. Check Razorpay popup shows: ₹1,499.00 (NOT ₹201)


═══════════════════════════════════════════════════════════════

🧪 TEST PAYMENT (Optional - Use Test Mode)
───────────────────────────────────────────────────────────────

If Razorpay is in TEST mode, use these test cards:

Success:
  Card: 4111 1111 1111 1111
  CVV: Any 3 digits
  Expiry: Any future date

Failure:
  Card: 4000 0000 0000 0002
  CVV: Any 3 digits
  Expiry: Any future date


═══════════════════════════════════════════════════════════════

⚠️  IMPORTANT NOTES
───────────────────────────────────────────────────────────────

1. CLEAR BROWSER CACHE after deployment:
   • Hard refresh: Ctrl+Shift+R
   • Or use incognito mode

2. CHECK RAZORPAY MODE:
   • Test mode: Safe to test with test cards
   • Live mode: Real payments will be charged!

3. VERIFY BACKEND .env:
   • RAZORPAY_KEY_ID should match mode (test/live)
   • RAZORPAY_KEY_SECRET should match mode (test/live)


═══════════════════════════════════════════════════════════════

📊 PAYMENT AMOUNTS
───────────────────────────────────────────────────────────────

Plan          | Display  | Razorpay | Razorpay (Paise)
--------------|----------|----------|------------------
Pro Plan      | ₹1,000   | 1000     | 100000
Premium Plan  | ₹1,499   | 1499     | 149900


═══════════════════════════════════════════════════════════════

🔄 ONE-LINE DEPLOY (Copy all at once)
───────────────────────────────────────────────────────────────

ssh root@72.60.218.219 "cd /root/Nextute-main && git pull origin main && cd frontend && rm -rf dist && npm run build && sudo cp -r dist/* /var/www/nextute/ && sudo systemctl restart nginx && cd .. && pm2 restart all && pm2 status"


═══════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING
───────────────────────────────────────────────────────────────

Problem: Still showing old prices (₹101/₹201)
Solution:
  1. Clear browser cache completely
  2. Hard refresh: Ctrl+Shift+R
  3. Try incognito/private mode
  4. Check frontend build date: ls -lh frontend/dist/index.html

Problem: Razorpay not opening
Solution:
  1. Check browser console for errors (F12)
  2. Verify Razorpay script loaded
  3. Check backend logs: pm2 logs backend

Problem: Payment fails
Solution:
  1. Check Razorpay dashboard for error details
  2. Verify Razorpay keys in backend/.env
  3. Check backend logs: pm2 logs backend --err


═══════════════════════════════════════════════════════════════

📞 SUPPORT
───────────────────────────────────────────────────────────────

Files to check:
  • RAZORPAY_PRICING_FIX.md - Detailed fix documentation
  • frontend/src/pages/MentorshipComingSoon.jsx - Line 38
  • frontend/src/pages/MentorDetailPage.jsx - Line 35

Logs to check:
  • pm2 logs backend
  • Razorpay dashboard
  • Browser console (F12)


═══════════════════════════════════════════════════════════════

✅ DEPLOYMENT CHECKLIST
───────────────────────────────────────────────────────────────

Before deployment:
  □ Code committed to Git
  □ Razorpay keys verified in backend/.env

After deployment:
  □ Frontend rebuilt
  □ Files copied to nginx
  □ PM2 restarted
  □ Browser cache cleared
  □ Pro Plan shows ₹1,000 in Razorpay
  □ Premium Plan shows ₹1,499 in Razorpay
  □ Test payment works (if in test mode)


═══════════════════════════════════════════════════════════════

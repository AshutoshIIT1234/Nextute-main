═══════════════════════════════════════════════════════════════
  DEPLOY ALL CHANGES TO VPS - COMPLETE UPDATE
  VPS: 72.60.218.219
═══════════════════════════════════════════════════════════════

📦 WHAT NEEDS TO BE DEPLOYED:
───────────────────────────────────────────────────────────────
✅ Backend CORS fix (allows nextute.com and www.nextute.com)
✅ Frontend pricing updates (Early Bird offers)
✅ Razorpay pricing fix (₹1,000 and ₹1,499)
✅ Early Bird popup with GIFs
✅ Testimonial infinite loop
✅ Email improvements
✅ Calendly links for mentors
✅ 403 error fixes
✅ Debug endpoints


═══════════════════════════════════════════════════════════════

🚀 OPTION 1: AUTOMATED COMPLETE UPDATE (RECOMMENDED)
───────────────────────────────────────────────────────────────

Run on LOCAL machine:
───────────────────────────────────────────────────────────────
chmod +x complete-vps-update.sh
./complete-vps-update.sh


This will:
  • Stop all services
  • Backup current version
  • Pull latest code
  • Update backend
  • Rebuild frontend completely
  • Deploy to nginx
  • Clear all caches
  • Restart services
  • Verify everything


═══════════════════════════════════════════════════════════════

🚀 OPTION 2: MANUAL STEP-BY-STEP
───────────────────────────────────────────────────────────────

STEP 1: Connect to VPS
───────────────────────────────────────────────────────────────
ssh root@72.60.218.219


STEP 2: Navigate and backup
───────────────────────────────────────────────────────────────
cd /root/Nextute-main
cp -r . ../Nextute-backup-$(date +%Y%m%d)


STEP 3: Pull latest code
───────────────────────────────────────────────────────────────
git fetch origin
git reset --hard origin/main


STEP 4: Update backend
───────────────────────────────────────────────────────────────
cd backend
npm install
npx prisma db push
node prisma/seed-mentors.js


STEP 5: Nuclear frontend rebuild
───────────────────────────────────────────────────────────────
cd ../frontend
rm -rf node_modules dist .vite package-lock.json
npm cache clean --force
npm install --force
npm run build


STEP 6: Deploy to nginx
───────────────────────────────────────────────────────────────
sudo rm -rf /var/www/nextute/*
sudo cp -r dist/* /var/www/nextute/
sudo chown -R www-data:www-data /var/www/nextute
sudo chmod -R 755 /var/www/nextute


STEP 7: Clear caches and restart
───────────────────────────────────────────────────────────────
sudo rm -rf /var/cache/nginx/*
sudo systemctl restart nginx
cd ..
pm2 restart all
pm2 save


STEP 8: Verify
───────────────────────────────────────────────────────────────
pm2 status
pm2 logs backend --lines 20
curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════

⚡ OPTION 3: ONE-LINE NUCLEAR UPDATE
───────────────────────────────────────────────────────────────

ssh root@72.60.218.219 "cd /root/Nextute-main && pm2 stop all && git fetch origin && git reset --hard origin/main && cd backend && npm install && npx prisma db push && cd ../frontend && rm -rf node_modules dist .vite && npm cache clean --force && npm install --force && npm run build && sudo rm -rf /var/www/nextute/* && sudo cp -r dist/* /var/www/nextute/ && sudo chown -R www-data:www-data /var/www/nextute && sudo rm -rf /var/cache/nginx/* && sudo systemctl restart nginx && cd .. && pm2 restart all && pm2 save && pm2 status"


═══════════════════════════════════════════════════════════════

⚠️  AFTER DEPLOYMENT - CRITICAL!
───────────────────────────────────────────────────────────────

YOU MUST CLEAR YOUR BROWSER CACHE!

Method 1: Hard Refresh (Fastest)
───────────────────────────────────────────────────────────────
Windows: Ctrl + Shift + R
Mac:     Cmd + Shift + R


Method 2: Clear All Cache
───────────────────────────────────────────────────────────────
1. Press Ctrl+Shift+Delete (Cmd+Shift+Delete on Mac)
2. Select "Cached images and files"
3. Select "All time"
4. Click "Clear data"


Method 3: Use Incognito/Private Mode
───────────────────────────────────────────────────────────────
Chrome/Edge: Ctrl+Shift+N
Firefox:     Ctrl+Shift+P
Safari:      Cmd+Shift+N


═══════════════════════════════════════════════════════════════

✅ VERIFY ALL CHANGES
───────────────────────────────────────────────────────────────

1. Visit: https://www.nextute.com
2. Open DevTools (F12) → Console tab
3. Check for errors:
   ✓ NO CORS errors
   ✓ NO 503 errors
   ✓ NO 404 errors for GIFs

4. Go to Mentorship page:
   ✓ Pro Plan shows: ₹1,500 → ₹1,000 (Early Bird)
   ✓ Premium Plan shows: ₹1,999 → ₹1,499 (Early Bird)

5. Click "Book Session":
   ✓ Razorpay shows ₹1,000 for Pro (not ₹101)
   ✓ Razorpay shows ₹1,499 for Premium (not ₹201)

6. Wait 2 seconds on homepage:
   ✓ Early Bird popup appears
   ✓ Shows correct GIF (desktop or mobile)
   ✓ Click GIF → Goes to mentorship page

7. Check Testimonials section:
   ✓ Testimonials loop infinitely

8. Test signup:
   ✓ OTP email arrives
   ✓ No 403 errors after login


═══════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING
───────────────────────────────────────────────────────────────

Problem: Frontend still shows old version
Solution:
  1. CLEAR BROWSER CACHE (most common issue)
  2. Hard refresh: Ctrl+Shift+R
  3. Try incognito mode
  4. Check build date on VPS:
     ls -lh /var/www/nextute/index.html

Problem: CORS errors still appear
Solution:
  1. Check backend is running: pm2 status
  2. Restart backend: pm2 restart backend
  3. Check logs: pm2 logs backend

Problem: Early Bird popup not showing
Solution:
  1. Clear sessionStorage:
     sessionStorage.removeItem("earlyBirdPopupSeen")
  2. Refresh page
  3. Check console for errors

Problem: Razorpay shows old prices
Solution:
  1. Frontend not rebuilt properly
  2. Run nuclear frontend update again
  3. Clear browser cache

Problem: 503 errors
Solution:
  1. Backend is down
  2. Check: pm2 status
  3. Restart: pm2 restart backend
  4. Check logs: pm2 logs backend --err


═══════════════════════════════════════════════════════════════

📊 QUICK DIAGNOSTIC
───────────────────────────────────────────────────────────────

Run on VPS to check everything:
───────────────────────────────────────────────────────────────
cd /root/Nextute-main && \
echo "=== GIT STATUS ===" && \
git log -1 --oneline && \
echo -e "\n=== FRONTEND BUILD ===" && \
ls -lh frontend/dist/index.html && \
echo -e "\n=== NGINX FILES ===" && \
ls -lh /var/www/nextute/index.html && \
echo -e "\n=== PM2 STATUS ===" && \
pm2 status && \
echo -e "\n=== BACKEND TEST ===" && \
curl -s http://localhost:8080/test && \
echo -e "\n\n=== PRICING CHECK ===" && \
grep -o "1000\|1499" frontend/dist/assets/*.js | head -5


═══════════════════════════════════════════════════════════════

🔄 ROLLBACK (If something goes wrong)
───────────────────────────────────────────────────────────────

ssh root@72.60.218.219

pm2 stop all
cd /root
rm -rf Nextute-main
mv Nextute-backup-YYYYMMDD Nextute-main
cd Nextute-main
pm2 restart all


═══════════════════════════════════════════════════────────════

📞 SUPPORT FILES
───────────────────────────────────────────────────────────────

Detailed guides:
  • FIX_CORS_NOW.txt - CORS issue fix
  • UPDATE_RAZORPAY_PRICING.txt - Razorpay pricing
  • ADD_EARLY_BIRD_POPUP.txt - Early Bird popup
  • FIX_FRONTEND_NOW.txt - Frontend update issues
  • WHY_NOT_UPDATED.md - Troubleshooting guide


═══════════════════════════════════════════════════════════════

💡 REMEMBER:
───────────────────────────────────────────────────────────────

The #1 reason for "not updated" is BROWSER CACHE!

Always:
  1. Clear browser cache after deployment
  2. Hard refresh (Ctrl+Shift+R)
  3. Test in incognito mode first


═══════════════════════════════════════════════════════════════

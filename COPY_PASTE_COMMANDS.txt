═══════════════════════════════════════════════════════════════
  NEXTUTE VPS UPDATE - COPY & PASTE THESE COMMANDS
═══════════════════════════════════════════════════════════════

📋 STEP 1: SSH INTO VPS
───────────────────────────────────────────────────────────────
ssh your-user@your-vps-ip


📋 STEP 2: NAVIGATE TO PROJECT
───────────────────────────────────────────────────────────────
cd /path/to/Nextute-main


📋 STEP 3: CREATE BACKUP
───────────────────────────────────────────────────────────────
cp -r . ../Nextute-backup-$(date +%Y%m%d-%H%M%S)


📋 STEP 4: PULL LATEST CODE
───────────────────────────────────────────────────────────────
git fetch origin
git pull origin main


📋 STEP 5: UPDATE BACKEND
───────────────────────────────────────────────────────────────
cd backend
npm install
npx prisma db push
node prisma/seed-mentors.js


📋 STEP 6: UPDATE FRONTEND
───────────────────────────────────────────────────────────────
cd ../frontend
npm install
npm run build


📋 STEP 7: RESTART SERVICES
───────────────────────────────────────────────────────────────
pm2 restart all


📋 STEP 8: CHECK STATUS
───────────────────────────────────────────────────────────────
pm2 status
pm2 logs backend --lines 20


📋 STEP 9: TEST BACKEND
───────────────────────────────────────────────────────────────
curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════
  IF ABOVE DOESN'T WORK - USE FORCE UPDATE
═══════════════════════════════════════════════════════════════

📋 FORCE UPDATE (Copy all at once)
───────────────────────────────────────────────────────────────
pm2 stop all && \
pm2 delete all && \
rm -rf backend/node_modules frontend/node_modules frontend/dist && \
git fetch origin && \
git reset --hard origin/main && \
cd backend && \
npm install && \
npx prisma generate && \
npx prisma db push && \
node prisma/seed-mentors.js && \
cd ../frontend && \
npm install && \
npm run build && \
cd .. && \
pm2 start ecosystem.config.js && \
pm2 save && \
sleep 5 && \
pm2 status && \
curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════
  VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════

📋 CHECK PM2 STATUS
───────────────────────────────────────────────────────────────
pm2 status


📋 VIEW LOGS
───────────────────────────────────────────────────────────────
pm2 logs backend --lines 50


📋 CHECK MENTORS API (Should show calendlyLink)
───────────────────────────────────────────────────────────────
curl http://localhost:8080/api/mentorship/mentors


📋 TEST EMAIL
───────────────────────────────────────────────────────────────
cd backend
node test-email.js


📋 CHECK FRONTEND BUILD DATE
───────────────────────────────────────────────────────────────
ls -la frontend/dist/index.html


═══════════════════════════════════════════════════════════════
  IF USING NGINX - COPY FRONTEND BUILD
═══════════════════════════════════════════════════════════════

📋 COPY BUILD TO NGINX
───────────────────────────────────────────────────────────────
sudo cp -r frontend/dist/* /var/www/nextute/
sudo systemctl restart nginx


═══════════════════════════════════════════════════════════════
  ROLLBACK (If something goes wrong)
═══════════════════════════════════════════════════════════════

📋 RESTORE BACKUP
───────────────────────────────────────────────────────────────
pm2 stop all
cd /path/to
rm -rf Nextute-main
mv Nextute-backup-YYYYMMDD-HHMMSS Nextute-main
cd Nextute-main
pm2 restart all


═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

📋 CHECK WHAT'S USING PORT 8080
───────────────────────────────────────────────────────────────
sudo lsof -i :8080


📋 KILL PROCESS ON PORT 8080
───────────────────────────────────────────────────────────────
sudo kill -9 $(lsof -t -i:8080)


📋 CHECK GIT STATUS
───────────────────────────────────────────────────────────────
git log -1 --oneline
git status


📋 CHECK DATABASE CONNECTION
───────────────────────────────────────────────────────────────
cd backend
npx prisma db pull


📋 VIEW ERROR LOGS ONLY
───────────────────────────────────────────────────────────────
pm2 logs backend --err --lines 50


═══════════════════════════════════════════════════════════════
  WHAT TO CHECK ON WEBSITE
═══════════════════════════════════════════════════════════════

1. Visit: https://www.nextute.com
2. Check Testimonials - Should loop infinitely
3. Go to Mentorship page
4. Check pricing:
   ✓ Pro Plan: ₹1,500 → ₹1,000 (Early Bird)
   ✓ Premium Plan: ₹1,999 → ₹1,499 (Early Bird)
5. Test signup - OTP should arrive
6. Test login - No 403 errors


═══════════════════════════════════════════════════════════════
  QUICK DIAGNOSTIC
═══════════════════════════════════════════════════════════════

📋 RUN ALL CHECKS AT ONCE
───────────────────────────────────────────────────────────────
echo "=== PM2 Status ===" && \
pm2 status && \
echo -e "\n=== Backend Health ===" && \
curl http://localhost:8080/test && \
echo -e "\n\n=== Git Status ===" && \
git log -1 --oneline && \
echo -e "\n=== Frontend Build ===" && \
ls -la frontend/dist/index.html && \
echo -e "\n=== Recent Logs ===" && \
pm2 logs backend --lines 10 --nostream


═══════════════════════════════════════════════════════════════

💡 TIP: If you see "not updated" on website:
   1. Hard refresh browser: Ctrl+Shift+R
   2. Try incognito/private mode
   3. Clear browser cache
   4. Check if nginx needs restart

═══════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════
  FIX CORS ISSUE - IMMEDIATE DEPLOYMENT
  VPS: 72.60.218.219
═══════════════════════════════════════════════════════════════

🚨 PROBLEM:
───────────────────────────────────────────────────────────────
CORS error when accessing API from https://nextute.com
Backend only allowed https://www.nextute.com


🔧 SOLUTION:
───────────────────────────────────────────────────────────────
Updated backend to allow BOTH:
  • https://nextute.com (without www)
  • https://www.nextute.com (with www)


═══════════════════════════════════════════════════════════════

🚀 DEPLOY FIX NOW (Copy-paste these commands)
───────────────────────────────────────────────────────────────

STEP 1: Connect to VPS
───────────────────────────────────────────────────────────────
ssh root@72.60.218.219


STEP 2: Update code and restart backend
───────────────────────────────────────────────────────────────
cd /root/Nextute-main
git pull origin main
pm2 restart backend


STEP 3: Verify fix
───────────────────────────────────────────────────────────────
pm2 logs backend --lines 20
curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════

⚡ ONE-LINE FIX (Copy all at once)
───────────────────────────────────────────────────────────────
ssh root@72.60.218.219 "cd /root/Nextute-main && git pull origin main && pm2 restart backend && pm2 status"


═══════════════════════════════════════════════════════════════

✅ TEST AFTER DEPLOYMENT
───────────────────────────────────────────────────────────────

1. Visit: https://nextute.com (without www)
2. Check browser console (F12)
3. Should NOT see CORS errors
4. API calls should work

Also test:
5. Visit: https://www.nextute.com (with www)
6. Should also work without errors


═══════════════════════════════════════════════════════════════

🐛 IF 503 ERROR PERSISTS
───────────────────────────────────────────────────────────────

The 503 error means backend is not running.

Check backend status:
───────────────────────────────────────────────────────────────
pm2 status


If backend is stopped:
───────────────────────────────────────────────────────────────
pm2 restart backend


If backend keeps crashing:
───────────────────────────────────────────────────────────────
pm2 logs backend --err --lines 50


Check what's using port 8080:
───────────────────────────────────────────────────────────────
sudo lsof -i :8080


Kill process if needed:
───────────────────────────────────────────────────────────────
sudo kill -9 $(lsof -t -i:8080)
pm2 restart backend


═══════════════════════════════════════════════════════════════

📊 VERIFY BACKEND IS RUNNING
───────────────────────────────────────────────────────────────

# Check PM2 status
pm2 status

# Should show:
# backend | online | ...

# Test API directly
curl http://localhost:8080/test

# Should return:
# {"status":true,"message":"Server is running!"}


═══════════════════════════════════════════════════════════════

🔍 TROUBLESHOOTING
───────────────────────────────────────────────────────────────

Problem: Still getting CORS error
Solution:
  1. Clear browser cache (Ctrl+Shift+R)
  2. Try incognito mode
  3. Check pm2 logs: pm2 logs backend

Problem: 503 Service Unavailable
Solution:
  1. Backend is not running
  2. Check: pm2 status
  3. Restart: pm2 restart backend
  4. Check logs: pm2 logs backend --err

Problem: Backend won't start
Solution:
  1. Check logs: pm2 logs backend --err
  2. Check port: sudo lsof -i :8080
  3. Check .env file exists: ls -la backend/.env
  4. Check database connection


═══════════════════════════════════════════════════════════════

💡 RECOMMENDED: SET UP REDIRECT
───────────────────────────────────────────────────────────────

To avoid confusion, redirect one domain to the other:

Option 1: Redirect www to non-www
───────────────────────────────────────────────────────────────
# Edit nginx config
sudo nano /etc/nginx/sites-enabled/default

# Add this server block:
server {
    server_name www.nextute.com;
    return 301 https://nextute.com$request_uri;
}

# Test and restart
sudo nginx -t
sudo systemctl restart nginx


Option 2: Redirect non-www to www
───────────────────────────────────────────────────────────────
# Edit nginx config
sudo nano /etc/nginx/sites-enabled/default

# Add this server block:
server {
    server_name nextute.com;
    return 301 https://www.nextute.com$request_uri;
}

# Test and restart
sudo nginx -t
sudo systemctl restart nginx


═══════════════════════════════════════════════════════════════

✅ DEPLOYMENT CHECKLIST
───────────────────────────────────────────────────────────────

After deployment:
  □ Backend restarted
  □ PM2 shows "online"
  □ No CORS errors on https://nextute.com
  □ No CORS errors on https://www.nextute.com
  □ API calls work from both domains
  □ No 503 errors


═══════════════════════════════════════════════════════════════

📞 QUICK REFERENCE
───────────────────────────────────────────────────────────────

Connect:     ssh root@72.60.218.219
Update:      cd /root/Nextute-main && git pull origin main
Restart:     pm2 restart backend
Status:      pm2 status
Logs:        pm2 logs backend
Test API:    curl http://localhost:8080/test


═══════════════════════════════════════════════════════════════

╔═══════════════════════════════════════════════════════════════════════════╗
║                    TECH HUNT EVENT - COMPLETE FLOW                        ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          USER JOURNEY                                    │
└─────────────────────────────────────────────────────────────────────────┘

    👤 Student
     │
     ├─► Visits: https://www.nextute.com/tech-hunt
     │
     ├─► Sees:
     │    • Event banner with "Tech Hunt 2025"
     │    • Real-time stats (150 participants, 45 teams)
     │    • Benefits cards (Rewards, Limited Offer, Team Benefits)
     │    • Claim form
     │
     ├─► Fills form:
     │    • Name: "Rahul Kumar" ✓
     │    • Roll Number: "2021CS042" ✓
     │    • Team Name: "Code Ninjas" ✓
     │    • Email: "rahul@example.com" (optional)
     │    • Phone: "+91 9876543210" (optional)
     │    • College: "IIT Delhi" (optional)
     │
     ├─► Clicks: "🎉 Claim My Reward Now!"
     │
     ├─► Loading... (spinner animation)
     │
     ├─► Success! ✅
     │    • Animated checkmark appears
     │    • Confetti animation
     │    • Shows submitted details
     │    • Message: "Congratulations! 🎉"
     │
     └─► Can click "Back to Home"


┌─────────────────────────────────────────────────────────────────────────┐
│                        TECHNICAL FLOW                                    │
└─────────────────────────────────────────────────────────────────────────┘

Frontend                    Backend                     Database
(React)                     (Express)                   (PostgreSQL)
   │                           │                            │
   │  1. Load page             │                            │
   ├──────────────────────────►│                            │
   │  GET /tech-hunt           │                            │
   │                           │                            │
   │  2. Fetch stats           │                            │
   ├──────────────────────────►│  3. Query stats           │
   │  GET /api/tech-hunt/stats │───────────────────────────►│
   │                           │                            │
   │                           │  4. Return stats           │
   │  5. Display stats         │◄───────────────────────────┤
   │◄──────────────────────────┤                            │
   │  { participants: 150,     │                            │
   │    teams: 45 }            │                            │
   │                           │                            │
   │  6. User fills form       │                            │
   │  and submits              │                            │
   │                           │                            │
   │  7. POST request          │                            │
   ├──────────────────────────►│  8. Validate data         │
   │  POST /api/tech-hunt/claim│  • Check required fields  │
   │  {                        │  • Validate formats       │
   │    name: "Rahul",         │                            │
   │    rollNumber: "2021CS",  │  9. Check duplicate       │
   │    teamName: "Ninjas"     │───────────────────────────►│
   │  }                        │  WHERE roll_number =       │
   │                           │    "2021CS"                │
   │                           │  AND team_name = "Ninjas"  │
   │                           │                            │
   │                           │  10. No duplicate found    │
   │                           │◄───────────────────────────┤
   │                           │                            │
   │                           │  11. Insert record         │
   │                           │───────────────────────────►│
   │                           │  INSERT INTO               │
   │                           │  tech_hunt_participants    │
   │                           │                            │
   │                           │  12. Return new record     │
   │                           │◄───────────────────────────┤
   │                           │                            │
   │  13. Success response     │                            │
   │◄──────────────────────────┤                            │
   │  {                        │                            │
   │    status: true,          │                            │
   │    message: "Success!",   │                            │
   │    data: { ... }          │                            │
   │  }                        │                            │
   │                           │                            │
   │  14. Show success UI      │                            │
   │  • Animate checkmark      │                            │
   │  • Display details        │                            │
   │  • Update stats           │                            │
   │                           │                            │


┌─────────────────────────────────────────────────────────────────────────┐
│                      DUPLICATE SUBMISSION FLOW                           │
└─────────────────────────────────────────────────────────────────────────┘

Frontend                    Backend                     Database
   │                           │                            │
   │  1. User submits again    │                            │
   │  with same roll number    │                            │
   │  and team name            │                            │
   │                           │                            │
   │  2. POST request          │                            │
   ├──────────────────────────►│  3. Check duplicate       │
   │  POST /api/tech-hunt/claim│───────────────────────────►│
   │  {                        │  WHERE roll_number =       │
   │    rollNumber: "2021CS",  │    "2021CS"                │
   │    teamName: "Ninjas"     │  AND team_name = "Ninjas"  │
   │  }                        │                            │
   │                           │  4. Found existing!        │
   │                           │◄───────────────────────────┤
   │                           │                            │
   │  5. Error response        │                            │
   │◄──────────────────────────┤                            │
   │  {                        │                            │
   │    status: false,         │                            │
   │    message: "Already      │                            │
   │      claimed!",           │                            │
   │    error: "ALREADY_       │                            │
   │      CLAIMED"             │                            │
   │  }                        │                            │
   │                           │                            │
   │  6. Show error toast      │                            │
   │  "You have already        │                            │
   │   claimed your reward!"   │                            │
   │                           │                            │


┌─────────────────────────────────────────────────────────────────────────┐
│                         FILE STRUCTURE                                   │
└─────────────────────────────────────────────────────────────────────────┘

Nextute-main/
│
├── backend/
│   ├── controllers/
│   │   └── techHuntController.js ◄─── API logic
│   ├── routes/
│   │   └── techHuntRoutes.js ◄─────── Route definitions
│   ├── prisma/
│   │   └── schema.prisma ◄─────────── Database schema
│   └── server.js ◄────────────────── Import routes
│
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   └── TechHuntSponsor.jsx ◄─ Main page component
│   │   └── App.jsx ◄──────────────── Add route
│   └── ...
│
├── deploy-tech-hunt.sh ◄──────────── Linux/Mac deployment
├── deploy-tech-hunt.bat ◄──────────── Windows deployment
├── test-tech-hunt-local.bat ◄──────── Local testing
│
└── Documentation/
    ├── TECH_HUNT_README.md ◄────────── Complete guide
    ├── TECH_HUNT_SUMMARY.md ◄───────── Quick summary
    ├── TECH_HUNT_PREVIEW.md ◄───────── Visual preview
    └── TECH_HUNT_QUICK_REF.txt ◄────── Quick reference


┌─────────────────────────────────────────────────────────────────────────┐
│                      DATABASE SCHEMA                                     │
└─────────────────────────────────────────────────────────────────────────┘

tech_hunt_participants
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ Column       │ Type         │ Constraints  │ Description  │
├──────────────┼──────────────┼──────────────┼──────────────┤
│ id           │ UUID         │ PRIMARY KEY  │ Unique ID    │
│ name         │ VARCHAR(255) │ NOT NULL     │ Full name    │
│ roll_number  │ VARCHAR(100) │ NOT NULL     │ Roll number  │
│ team_name    │ VARCHAR(255) │ NOT NULL     │ Team name    │
│ email        │ VARCHAR(255) │ NULLABLE     │ Email        │
│ phone        │ VARCHAR(20)  │ NULLABLE     │ Phone        │
│ college      │ VARCHAR(255) │ NULLABLE     │ College      │
│ claimed_at   │ TIMESTAMP    │ DEFAULT NOW  │ Claim time   │
│ created_at   │ TIMESTAMP    │ DEFAULT NOW  │ Created      │
│ updated_at   │ TIMESTAMP    │ AUTO UPDATE  │ Updated      │
└──────────────┴──────────────┴──────────────┴──────────────┘

UNIQUE CONSTRAINT: (roll_number, team_name)
INDEX: claimed_at (for sorting)


┌─────────────────────────────────────────────────────────────────────────┐
│                      API ENDPOINTS DETAIL                                │
└─────────────────────────────────────────────────────────────────────────┘

1. POST /api/tech-hunt/claim
   ├─ Purpose: Submit reward claim
   ├─ Auth: None (public)
   ├─ Body: { name, rollNumber, teamName, email?, phone?, college? }
   ├─ Success: 201 Created
   ├─ Duplicate: 409 Conflict
   └─ Error: 400 Bad Request / 500 Server Error

2. GET /api/tech-hunt/stats
   ├─ Purpose: Get participation statistics
   ├─ Auth: None (public)
   ├─ Response: { totalParticipants, totalTeams }
   └─ Status: 200 OK

3. GET /api/tech-hunt/participants
   ├─ Purpose: Get all participants
   ├─ Auth: None (should add admin auth)
   ├─ Response: Array of all participants
   └─ Status: 200 OK


┌─────────────────────────────────────────────────────────────────────────┐
│                      DEPLOYMENT STEPS                                    │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: Update Database Schema
   └─► npx prisma db push
       └─► Creates tech_hunt_participants table

Step 2: Install Dependencies
   ├─► Backend: npm install (if new packages)
   └─► Frontend: npm install (if new packages)

Step 3: Build Frontend
   └─► npm run build
       └─► Creates optimized production bundle

Step 4: Restart Services
   └─► pm2 restart all
       └─► Restarts backend and frontend

Step 5: Verify
   ├─► Test backend: curl localhost:8080/api/tech-hunt/stats
   └─► Test frontend: Visit /tech-hunt


┌─────────────────────────────────────────────────────────────────────────┐
│                      SUCCESS METRICS                                     │
└─────────────────────────────────────────────────────────────────────────┘

Track these:
├─► Total participants (real-time)
├─► Total teams (real-time)
├─► Submissions per hour
├─► Most common colleges
├─► Peak submission times
└─► Duplicate attempt rate


╔═══════════════════════════════════════════════════════════════════════════╗
║  Ready to Deploy! Run: deploy-tech-hunt.bat                               ║
╚═══════════════════════════════════════════════════════════════════════════╝
